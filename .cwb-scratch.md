<cb desc="Prompt: generate project description + rebuild spec" lang="markdown">
Run both `/describe-project` and `/rebuild-spec` on this project. Output them as two separate documents. Run the description skill first, then the rebuild spec — the description gives you orientation context that helps produce a better spec.

Write both outputs to `.cwb-scratch.md` as separate `<cb>` blocks:
1. First block: `<cb desc="Project description" lang="markdown">` — the high-level description
2. Second block: `<cb desc="Project rebuild specification" lang="markdown">` — the rebuild spec

If this project doesn't use `.cwb-scratch.md`, output both documents directly in the conversation with clear `---` separators between them.
</cb>
